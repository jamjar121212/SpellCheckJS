{
  "name": "SpellCheck",
  "version": "0.0.0",
  "description": "Example of using a Trie data structure to perform spell checking and suggestions",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "James Connelly",
  "devDependencies": {
    "browserify": "~10.2.1",
    "watchify": "~3.2.1"
  }
}
